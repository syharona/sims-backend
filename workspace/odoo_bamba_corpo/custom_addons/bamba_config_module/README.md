# bamba_config - Configuration module for Bamba Corporation

This module centralizes configuration (custom fields, sequences, approvals templates, CRM stages, project templates).
Install it after core modules (crm, project, approvals, documents) are installed.
